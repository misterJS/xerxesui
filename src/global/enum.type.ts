export enum Size {
  mobile,
  tablet,
  pc,
  superpc,
}

export enum ButtonSize {
  small,
  medium,
  large,
  custom,
}

export enum Font {
  kids,
  basic,
  aesthetic,
  retro,
}

export enum FontType {
  h1,
  h2,
  h3,
  standard,
}
